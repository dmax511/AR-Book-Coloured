services:
  - type: web
    name: ar-book-server
    env: python
    buildCommand: "npm install && pip install -r requirements.txt"
    startCommand: "python server.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: NODE_VERSION
        value: 18.17.0
